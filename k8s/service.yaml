apiVersion: v1
kind: Service
metadata:
  name: container-lab
  labels:
    app: container-lab
spec:
  selector:
    app: container-lab
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
